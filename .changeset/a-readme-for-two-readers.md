---
"@eliya-oss/agent-figma": patch
---

The readme opens with what the CLI is and splits usage by who is running it: a human gets the tree, the comparison and table output; an agent gets `describe`, the JSON envelope, `--fields` and NDJSON, with the failure shape and exit codes stated once. Everything longer moved to `docs/`.
