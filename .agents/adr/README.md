# Architecture Decision Records

ADRs record durable technical decisions and their tradeoffs. Use `TEMPLATE.md` for new records.

ADR-007 defines the hosted Figma OAuth trust boundary and encrypted localhost handoff.
ADR-008 records that documentation ships as Markdown in `docs/`, superseding the ADR-006 site.
ADR-009 records releasing with pnpm 12's own tooling on a `beta` lane, over OIDC trusted publishing.
